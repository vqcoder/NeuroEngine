[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neurotrace_common"
version = "0.1.0"
description = "Shared Pydantic schemas for NeuroTrace"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7.0"]
